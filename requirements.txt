# ══════════════════════════════════════════════════════════════════════════════
# PaddleOCR Fine-tuning — Python dependencies
# ══════════════════════════════════════════════════════════════════════════════
#
# STEP 1 — Install PaddlePaddle BEFORE running pip install -r requirements.txt
#
#   CPU only (Linux / macOS / Windows):
#       pip install paddlepaddle==2.6.2
#
#   NVIDIA GPU (CUDA 11.7):
#       pip install paddlepaddle-gpu==2.6.1.post117 \
#           -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html
#
#   NVIDIA GPU (CUDA 11.8):
#       pip install paddlepaddle-gpu==2.6.1.post118 \
#           -f https://www.paddlepaddle.org.cn/whl/linux/mkl/avx/stable.html
#
#   If you installed the GPU variant on a CPU-only machine, switch with:
#       pip uninstall paddlepaddle-gpu -y && pip install paddlepaddle==2.6.2
#
#   Or use setup.sh which handles this automatically:
#       bash setup.sh [cpu|cu117|cu118]
#
# STEP 2 — Then install these requirements:
#       pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════════════════

# ── PaddleOCR ─────────────────────────────────────────────────────────────────
# 2.7.x ships the ppocr module (required by tools/train.py) and the inference API.
paddleocr>=2.7.0,<3.0.0

# ── Image processing ──────────────────────────────────────────────────────────
# opencv-python-headless must stay on 4.x — the 4.13+ wheel requires numpy>=2,
# which breaks PaddleOCR 2.7.x (compiled against numpy 1.x ABI).
opencv-python>=4.6.0,<4.10.0
opencv-python-headless>=4.6.0,<4.10.0
Pillow>=9.5.0

# ── Numerical / data ──────────────────────────────────────────────────────────
# PaddleOCR 2.7.x is compiled against numpy 1.x ABI — must stay below 2.0.
numpy>=1.24.0,<2.0.0
pandas>=1.5.0

# ── Excel I/O (required by pandas.read_excel for .xlsx files) ─────────────────
openpyxl>=3.0.9

# ── Evaluation ────────────────────────────────────────────────────────────────
# Used by PaddleOCRVLFinetuner.evaluate() to compute normalised edit-distance.
rapidfuzz>=3.0.0

# ── OCR error reporting ───────────────────────────────────────────────────────
# Used by TrainingDataPipeline.report_ocr_errors() to parse Markdown tables.
beautifulsoup4>=4.12.0

# ── YAML (needed by tools/train.py) ───────────────────────────────────────────
PyYAML>=6.0
pandas